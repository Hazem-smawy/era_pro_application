
    import 'package:flutter/material.dart';
    class SandPage extends StatelessWidget {
        const SandPage({super.key});
      
        @override
        Widget build(BuildContext context) {
          return Scaffold(
            appBar: AppBar(
              title: const Text('Sand Page'),
            ),
            );
            }
        }
    