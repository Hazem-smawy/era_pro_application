
    import 'package:flutter/material.dart';
    class SandPage extends StatelessWidget {
        const SandPage({Key? key}) : super(key: key);
      
        @override
        Widget build(BuildContext context) {
          return Scaffold(
            appBar: AppBar(
              title: Text('Sand Page'),
            ),
            );
            }
        }
    